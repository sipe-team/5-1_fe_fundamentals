describe('상품 목록', () => {
	it('API에서 상품 데이터를 불러와 카드 형태로 렌더링한다 (이미지, 상품명, 가격, 카테고리, 평점)', () => {});

	it('상품명이 길 경우에도 레이아웃이 깨지지 않는다', () => {});
});

describe('필터링', () => {
	describe('카테고리 필터', () => {
		it('체크박스로 shoes 카테고리를 선택하면 해당 상품만 표시된다', () => {});

		it('여러 카테고리를 다중 선택할 수 있다', () => {});

		it('선택한 카테고리를 해제하면 필터가 제거된다', () => {});
	});

	describe('키워드 검색', () => {
		it('검색어를 입력하면 해당 상품명을 포함한 상품만 표시된다', () => {});

		it('디바운싱이 적용되어 입력이 끝난 후 API를 호출한다', () => {});
	});
});

describe('검색 자동완성', () => {
	it('검색 input에 글자를 입력하면 자동완성 드롭다운이 표시된다', () => {});

	it('제안 항목 클릭 시 해당 검색어로 검색이 실행된다', () => {});

	it('제안 항목 선택 시 드롭다운이 닫힌다', () => {});

	it('외부 클릭 시 드롭다운이 닫힌다', () => {});

	it('Esc 키를 누르면 드롭다운이 닫힌다', () => {});
});

describe('정렬', () => {
	it('가격 낮은순으로 정렬할 수 있다', () => {});

	it('가격 높은순으로 정렬할 수 있다', () => {});

	it('최신순으로 정렬할 수 있다', () => {});

	it('평점순으로 정렬할 수 있다', () => {});
});

describe('URL 동기화', () => {
	it('필터/정렬/검색 상태가 URL 쿼리스트링에 반영된다', () => {});

	it('새로고침해도 필터 상태가 유지된다', () => {});

	it('URL을 통해 공유된 필터 상태가 그대로 적용된다', () => {});
});

describe('필터 초기화', () => {
	it('전체 초기화 버튼을 클릭하면 모든 필터가 기본값으로 리셋된다', () => {});
});

describe('상태 처리', () => {
	it('로딩 중일 때 로딩 표시가 보인다', () => {});

	it('API 에러 시 사용자 안내 메시지와 재시도 버튼이 표시된다', () => {});

	it('재시도 버튼 클릭 시 API를 다시 호출한다', () => {});

	it('필터 결과가 없을 때 빈 상태 안내가 표시된다', () => {});
});
